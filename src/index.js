import { addProperties, addGetters } from './addProperties'
import addEvents from './addEvents'
import addAnyEvents from './addAnyEvents'

export {
  addProperties,
  addGetters,
  addEvents,
  addAnyEvents
}
