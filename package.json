{
  "name": "properties-and-events",
  "description": "Convenience methods for adding properties and events with fluent api to objects",
  "version": "1.1.0",
  "license": "MIT",
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/lukebarlow/properties-and-events.git"
  },
  "files": [
    "dist",
    "lib",
    "src"
  ],
  "main": "lib/index.js",
  "scripts": {
    "test": "jest",
    "build": "babel src --out-dir lib",
    "prepublish": "npm run build && npm run test"
  },
  "dependencies": {
    "d3-dispatch": "^2.0.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.11.6",
    "@babel/core": "^7.11.6",
    "@babel/preset-env": "^7.11.5",
    "babel-jest": "^26.5.2",
    "jest": "^26.5.2"
  }
}
