{
  "name": "properties-and-events",
  "description": "Convenience methods for adding properties and events with fluent api to objects",
  "version": "1.0.1",
  "license": "MIT",
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/lukebarlow/properties-and-events.git"
  },
  "files": [
    "dist",
    "lib",
    "src"
  ],
  "main": "lib/index.js",
  "scripts": {
    "test": "jasmine ./src/specs/",
    "build": "cross-env BABEL_ENV=commonjs babel src --out-dir lib",
    "prepublish": "npm run test && npm run build",
    "standard": "standard --env jasmine",
    "standard:fix": "standard --env jasmine --fix"
  },
  "dependencies": {
    "d3-dispatch": "^1.0.3"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0",
    "cross-env": "^5.2.0",
    "jasmine-es6": "^0.4.3",
    "standard": "^11.0.1"
  }
}
